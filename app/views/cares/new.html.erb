<%= simple_form_for [@pet, @care] do |f| %>
  <div class="form-inputs">
    <div class="form-field">
      <%= f.input :category, collection: CATEGORIES, class: "input-text"%>
    </div>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Create" %>
  </div>
<% end %>
