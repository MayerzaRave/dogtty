<div class="row row-cols-3">
  <h3>My pet's 🐾</h3>
    <div class="col">
      <% @pets.each do |pet| %>
        <%= link_to pet.name, pet_path(pet) if pet.user_id == current_user.id%>
        <% end %>
    </div>
      <%= link_to "new pet", new_pet_path %>
</div>
